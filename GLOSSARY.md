# AI 工作台领域术语表（GLOSSARY）

本工程所有 PRD、需求文档、spec 中使用的领域术语以本表为准。定义力求紧凑：说清术语**是什么**，而不是它做什么。后续新增或澄清的领域术语统一补充到本表。

## 百炼平台侧

**百炼（Bailian）**：
阿里云大模型服务平台，本系统的上游能力提供方。
_避免_：DashScope 平台、阿里 AI 平台

**Agent（智能体应用）**：
在百炼平台发布的应用编排单元（知识库+提示词+工作流）。本系统通过 `appId` 调用它，不感知其内部编排。
_避免_：机器人、助手、应用（歧义时）

**sessionId（百炼会话标识）**：
百炼 Agent 侧维持多轮上下文的会话标识，由 Agent 响应返回。本系统将其回写到会话组，作为续聊依据。
_避免_：记忆 ID（memoryId 是另一个概念）、会话组 ID

**memoryId（记忆体标识）**：
百炼平台的长期记忆体资源标识。v1 不使用（不调用记忆体接口）。
_避免_：与 sessionId 混用

**dashscope-sdk-java**：
百炼官方 Java SDK，本系统通过其 `Application.streamCall` 发起流式调用。
_避免_：手写 OkHttp+SSE（老项目旧方案，已废弃）

## 本系统领域模型

**会话组（Conversation）**：
用户视角的一次完整对话主题，对应表 `ai_conversation`，主键 `conversationId`。归属创建用户，是数据隔离与计费统计的单元。
_避免_：会话、对话、session（与百炼 sessionId 冲突）

**对话内容（Conversation Content）**：
会话组内的一条消息（user 或 assistant），对应表 `ai_conversation_content`。每轮对话产生两条。
_避免_：消息记录、聊天记录

**一轮对话（Chat Turn）**：
一次用户输入及其触发的完整 Agent 回复过程。正常/断连/错误三种结局均产生落库记录。
_避免_：一次请求、一次交互

**增量分片（Incremental Chunk）**：
`incrementalOutput=true` 时百炼每次推送的文本片段，SSE `message` 事件逐个下发，前端自行拼接。
_避免_：全量文本、流片段

**隐式创建（Implicit Creation）**：
会话组不设独立创建接口，首轮对话（不传 `conversationId`）时自动建组。与「显式创建」相对。
_避免_：自动建会话

### 定时任务

**定时任务（Scheduled Task）**：
由业务用户配置名称、提示词和 cron 计划，并按计划周期性请求 Agent 的可管理任务。任务拥有唯一归属用户和绑定会话组。
_避免_：定时器、后台脚本

**任务执行轮次（Task Execution Run）**：
定时任务因一次调度触发或一次手动执行而产生的完整执行过程，从抢占成功开始，到执行成功、失败、超时或异常终止结束。
_避免_：任务请求、调度事件

**执行状态（Run State）**：
描述任务当前是否有执行轮次占用的控制状态。v1 只有 `IDLE`（空闲）和 `RUNNING`（执行中）两种状态；它不同于记录上一轮结果的成功/失败结局。
_避免_：任务状态（容易与启停状态或最近执行结局混淆）

**僵死任务（Stale Running Task）**：
执行状态持续为 `RUNNING`，且从本轮执行开始时间起超过 `runningStaleMinutes` 的任务。它表示任务可能因执行实例被终止而留下了未清理的执行状态，不等同于一次正常的执行失败。
_避免_：超时任务、失败任务（调用超时或调用失败有独立的失败结局）

**僵死复位（Stale-Run Reset）**：
将僵死任务的执行状态从 `RUNNING` 恢复为 `IDLE`，以释放后续执行资格的恢复动作。v1 复位不补跑，且必须保证不会覆盖已经结束或已被其他实例处理的执行轮次。
_避免_：重试、补偿执行

## 协议与错误

**SSE（Server-Sent Events）**：
`text/event-stream` 单向流式推送协议。本系统唯一的前端对话通道。
_避免_：WebSocket（双向，不采用）

**SSE 事件协议（Event Protocol）**：
本系统定义的三事件协议：`message`（增量分片）/ `error`（异常，含 code）/ `end`（正常结束 + 累计 token 汇总）。
_避免_：无名事件（现状旧方案）

**断连回收（Disconnect Cleanup）**：
客户端断开后，服务端取消百炼流订阅、停止拉取，并按「中断轮次」落库的过程。
_避免_：取消请求、清理连接

**中断轮次（Interrupted Turn）**：
因客户端断连被取消的一轮对话，`params` 标记 `{"interrupted":true,...}`，assistant 落库已收到的部分文本。
_避免_：失败轮次（那是 error 轮次）

**错误轮次（Error Turn）**：
百炼调用失败或空回复的一轮对话，`params` 标记 `{"error":true,"code":...}`。
_避免_：中断轮次、异常对话

**参数错误（PARAM_ERROR）**：
流式开始前的入参校验失败（`prompt` 空白/超 8000 字符、`type` 非 CHAT），走统一异常包装返回普通 JSON，不发 SSE。
_避免_：校验异常

## 角色与流程

**业务用户（Business User）**：
持有有效 JWT 的终端用户，会话组的唯一归属者。v1 唯一认证主体。
_避免_：租户用户、会员

**UserContext**：
认证过滤器解析 JWT 后写入的 ThreadLocal 用户上下文。跨线程（异步线程池）传递需「主线程捕获-异步线程重建」。
_避免_：会话上下文、AuthUser（那是过滤器）

**捕获-重建（Capture-Rebuild）**：
主线程读取 UserContext 快照、异步线程恢复的 ThreadLocal 传递模式，SSE 长任务的标准姿势。
_避免_：ThreadLocal 透传（未明确机制的说法）

## 客户画像领域

> 排期（2026-08-26 roadmap）：销售转化画像（customer-profile-sales）= **一期**基础版本；运营画像（customer-profile-ops）= **二期**。决策溯源见 `plans/customer-profile/customer-profile-roadmap-interview.md`。

**数据工具（Data Tool）**：
下游 MCP 统计服务暴露的、面向单一业务问题的客户数据查询工具，按业务问题切分而非按指标切分，带时间窗口参数（上限 365 天）。一期交付销售画像 3 工具；二期扩展至约 7~8 个（运营画像 + 对话式分析共同的数据获取层）。
_避免_：原子工具（易误解为按指标切分）、指标接口

**画像技能（Profiling Skill）**：
配置于百炼 Agent 侧、规定画像分析规则/输出结构/数据获取策略的指令资产；本仓库镜像文件为其**单一事实源**，变更走 PR review 后手动同步百炼。
_避免_：提示词（过于宽泛）、技能配置

**指标包（Metrics Bundle）**：
画像所需的指标全集的**逻辑概念**：v1 由 7 个数据工具的结果按画像技能规定的调用序列拼装而成，不再对应单一工具返回值。
_避免_：指标接口返回值（r2 前旧义：单工具一次返回的完整指标集合）

**平台画像客户（Profiled Customer）**：
已绑定店铺、进入画像覆盖范围的 B2B 代发客户，以平台客户 ID 标识。画像对全体内部用户可见，不服从业务用户归属隔离。
_避免_：业务用户（那是本系统登录主体）、店铺（一个客户可绑多店）

**活跃画像客户（Active Profiled Customer）**：
平台画像客户中近期（近 7 天）存在消费记录的子集，销售转化画像定时扫描的覆盖范围；手动触发不受此限。运营画像对象 ⊃ 活跃画像客户。
_避免_：销售线索（v1 已废弃：分析对象要求绑定+消费，非注册即入）、注册用户

**销售画像（Sales Profile）**：
面向销售转化的画像产出（客户画像一期）：三项 AI 评分（leadQuality/intentHeat/followPriority）+ 规则计算的 infoCompleteness + 线索速读/转化策略/销售话术。强校验失败即弃，无降级落库形态；覆盖式只留最新。
_避免_：线索画像（对象非线索）、FALLBACK 画像（已废弃的降级落库形态）

**运营画像（Ops Profile）**：
面向运营分层的画像产出（客户画像二期，模块 customer-profile-ops，表 `ai_customer_profile_ops`）：价值分层/生命周期/流失风险/主营品类/经营特征/建议动作 + 经营摘要。冷启动客户手动触发输出降级画像（仅 NEW + 基础摘要）；扫描路径自动跳过。
_避免_：客户画像（歧义——那是领域名，非单模块）、与销售画像混称
