# Grilling 决策记录 — 客户画像排期重整（roadmap）

> ⛔ **本排期已被取代（2026-08-26）**：客户画像域同日再次重整——sales/ops 两模块**整体废弃**，重做为「用户画像（customer-profile，新用户转化）」，一期/二期排期不再适用。新模块决策见 [customer-profile-interview.md](customer-profile-interview.md)。本文件仅作历史决策留痕。

> 日期：2026-08-26
> 范围：客户画像域跨模块排期决策（**不属单一模块**，两份 PRD 修订均引用本记录）
> 状态：已确认（三轮 18 个决策点，全部按推荐执行）
> 背景：销售转化画像 PRD（v1.2）来自**业务方提供**的需求；客户画像 AI 分析 PRD（v1.3）为内部补充需求。业务方需求更简单、可快速迭代，故销售画像提前为**客户画像一期基础版本**，运营画像整体后移至**二期**。

## 完整决策树

### Round 1 — 排期语义与文档处置

| # | 问题 | 推荐 | 实际答案 |
|---|------|------|---------|
| Q1 | 推迟运营画像的驱动因素 | （业务判断，不给推荐） | **ops PRD 是内部补充需求，sales PRD 来自业务方提供，业务需求优先；且内容更简单可快速迭代** |
| Q2 | 排期版本与模块版本命名区分 | (a) 产品线排期用「一期/二期」，模块内部保持 v1/v2 | **(a)** |
| Q3 | 前置母本倒挂处理 | sales 前置母本改 `scheduled-task-prd.md`（模式真正来源），ops 降为「关联模块（二期）」 | **按推荐** |
| Q4 | MCP 数据服务是否分期 | (a) 分期：一期仅交付 sales 3 工具，ops 契约冻结为二期输入 | **(a)** |
| Q5 | ops 已定版文档处置 | (a) 全部保留：PRD 加二期排期注记，requirements 标「二期冻结基线」，README/CHANGELOG 同步 | **(a) 全部保留** |
| Q6 | 模块名与表名是否维持 | (a) 不动（原推荐） | **改执行改名：ops → `customer-profile-ops`，后期好维护** |
| Q7 | CONTEXT.md 画像不变量归属矛盾 | (a) 生命周期与不变量按销售/运营画像拆分标注归属，降级画像明确仅属运营画像 | **按推荐** |
| Q8 | 二期启动条件 | (d) 双条件：运营侧正式认领 + MCP 统计服务立项并承诺工具交付 | **按推荐** |
| Q9 | 二期范围基线 | 二期 = ops PRD v1.3 现状范围；ops 内部 v2 项（分位数/历史版本/冷却期/主动推送）保持后置 | **按推荐** |
| Q10 | 本次访谈记录落位 | (a) 新建本文件（独立命名空间 Q1–Q18），两份 PRD 修订注记引用 | **按推荐** |

### Round 2 — 改名连锁决策（Q6 选执行改名后展开）

| # | 问题 | 推荐 | 实际答案 |
|---|------|------|---------|
| Q11 | ops 模块新名与表名 | (a) 模块 `customer-profile-ops`，目录 `plans/customer-profile/ops/`，表 `ai_customer_profile` → `ai_customer_profile_ops` | **全部按推荐** |
| Q12 | 改名触发的设计资产处置 | (a) `designs/customer-profile/ops/` 目录名本就是 ops，skill 镜像路径零变动 | **按推荐** |
| Q13 | GLOSSARY/CONTEXT 排期标注口径 | (a) 仅小节头加注记 + 不变量按归属拆分，术语定义保持纯净 | **按推荐** |
| Q14 | 一期是否预埋 ops 规则引擎基础设施 | 不预埋，二期纯增量开发（sales 扫描 = MCP 工具 3 取清单，非规则引擎） | **按推荐** |
| Q15 | sales PRD「对齐运营画像已验证模式」表述 | (a) 全部改写为指向 scheduled-task / architecture §4（模式真正来源，时间线不倒挂） | **按推荐** |
| Q16 | CHANGELOG 条目粒度 | (b) 2026-08-26 单条目，子项分列（排期/改名/引用同步） | **按推荐** |
| Q17 | sales PRD 版本号 | v1.2 → v1.3（修订级；「一期基础版本」由头部排期注记承载） | **按推荐** |
| Q18 | 二期重启时旧假设强制复核 | (a) ops PRD 加重启复核清单：T2 档位表/5 条扫描规则阈值/365 天窗口 | **按推荐** |

## 执行摘要（对应 2026-08-26 文档变更）

1. **排期**：customer-profile-sales = 客户画像**一期**基础版本；customer-profile-ops = **二期**（启动双条件见 Q8，范围基线见 Q9，重启复核清单见 Q18）
2. **改名归位**：ops 模块 `customer-profile` → `customer-profile-ops`；**表命名反转（同日补充决策）**——基名 `ai_customer_profile` 归**一期销售画像**，ops 二期建表用 `ai_customer_profile_ops`；plans 目录建 `ops/`、`sales/` 子目录（修正 README 目录树与实际的偏差及 sales PRD 前置母本断链旧账）
3. **MCP 分期**：一期仅 sales 3 工具；ops 7~8 工具契约冻结为二期输入
4. **模式溯源修正**：sales 前置母本改指 `scheduled-task-prd.md`，消除「复用未开发模块已验证模式」的时间线倒挂
5. **领域模型**：CONTEXT.md 画像不变量按销售/运营画像拆分归属（降级画像仅属运营画像；销售画像无降级落库形态）

## 溯源引用

- sales PRD 修订（v1.3）：`sales/customer-profile-sales-prd.md` 头部 r4 注记
- ops PRD 修订：`ops/customer-profile-ops-prd.md` 头部 r5 注记
